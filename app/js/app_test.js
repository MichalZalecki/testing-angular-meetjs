// Generated by CoffeeScript 1.9.0
(function() {
  'use strict';
  describe('myAppCtrl', function() {
    var scope;
    scope = null;
    beforeEach(module('myApp'));
    beforeEach(inject(function($rootScope, $controller) {
      scope = $rootScope.$new();
      $controller('myAppCtrl', {
        '$scope': scope,
        appName: 'FakeAppName'
      });
      return scope.$digest();
    }));
    it('should have initial msgs and appName values', function() {
      expect(scope.msgs).toEqual([]);
      return expect(scope.appName).toEqual('FakeAppName');
    });
    it('should have addMsg method', function() {
      return expect(scope.addMsg).toEqual(jasmine.any(Function));
    });
    return it('should add message with addMsg', function() {
      var msgsAfter, msgsBefore;
      msgsBefore = angular.copy(scope.msgs);
      scope.addMsg('success', 'Foo');
      msgsAfter = angular.copy(scope.msgs);
      expect(msgsAfter.length).toBeGreaterThan(msgsBefore);
      return expect(msgsAfter[0]).toEqual({
        style: 'success',
        text: 'Foo'
      });
    });
  });

}).call(this);
